<%- include('../partials/head') %>
<%- include('../partials/header') %>

<div class="container">
    <div class="row">
        <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
                <img src="<%= loveItem.image %>" class="card-img-top" alt="<%= loveItem.name %>">
                <div class="card-body">
                    <h5 class="card-title><%= loveItem.name %></h5>
                    <p class="card-text"><%= loveItem.description %></p>
                    <div class="d-flex justify-content-between align-items-center">
                        <div class="btn-group">
                            <a href="/order/<%= loveItem.id %>" class="btn btn-sm btn-outline-secondary">Order</a>
                        </div>
                        <small class="text-muted">💪<%= loveItem.energy_level %></small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>